action: create
data:
  table: recipes_steps
  definitions:
    columns:
      - name: recipe_id
        type:
          name: int
          unsigned: true
      - name: step_id
        type:
          name: int
          unsigned: true
      - name: order
        type:
          name: int
          unsigned: true
    references:
      - local: recipe_id
        table: recipes
        keys: id
        delete: cascade
        update: cascade
      - local: step_id
        table: steps
        keys: id
        delete: cascade
        update: cascade
