action: create
data:
  table: recipes_categories
  definitions:
    columns:
      - name: id
        type:
          name: int
          unsigned: true
        auto_increment: true
      - name: recipe_id
        type:
          name: int
          unsigned: true
        references:
          table: recipes
          keys: id
          delete: cascade
          update: cascade
      - name: category_id
        type:
          name: int
          unsigned: true
    primary: id
    references:
      #- local: recipe_id
      #  table: recipes
      #  keys: id
      #  delete: cascade
      #  update: cascade
      - local: category_id
        table: recipe_categories
        keys: id
        delete: cascade
        update: cascade
