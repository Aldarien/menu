action: create
data:
  table: categories_times
  definitions:
    columns:
      - name: category_id
        type:
          name: int
          unsigned: true
      - name: time
        type:
          name: int
          unsigned: true
          length: 1
    primary: time
    references:
      - local: category_id
        table: categories
        keys: id
        delete: cascade
        update: cascade
