action: create
data:
  table: steps
  definitions:
    columns:
      - name: id
        type:
          name: int
          unsigned: true
        primary: true
        auto_increment: true
      - name: method_id
        type:
          name: int
          unsigned: true
    references:
      - local: method_id
        table: methods
        keys: id
        delete: cascade
        update: cascade
